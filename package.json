{
  "name": "memory",
  "version": "1.0.0",
  "description": "A memory game built in elm",
  "main": "index.js",
  "scripts": {
    "start": "npm run start:client",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "elm make src/Main.elm --output=./assets/app.js",
    "start:client": "elm-live --dir=./assets src/Main.elm --open --start-page=index.html -- --output=assets/app.js"
  },
  "author": "Will Farrell",
  "license": "MIT",
  "dependencies": {
    "css-loader": "^1.0.0",
    "elm": "^0.19.0-bugfix2",
    "elm-format": "^0.8.1",
    "elm-upgrade": "^0.19.6"
  },
  "devDependencies": {
    "elm-live": "^3.2.1"
  }
}
